<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="styles.css">
    <title>User Directory</title>
  </head>
  <body>
    <h1 class="header">User Directory</h1>
    <div class="container">
    {{ #users}}
        <div class="user-box">
          <a href="/{{username}}"><img src="{{avatar}}" alt=""></a>
            <div class="">
              <h1>{{name}}</h1>
              {{#job}}
              <span>{{job}}</span><span>{{company}}</span>
              {{/job}}
              {{^job}}
              <span>Available for Hire.</span>
              {{/job}}
            </div>
        </div>
    {{ /users}}
    </div>
  </body>
</html>
